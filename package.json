{
  "name": "tenawork-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down",
    "install:all": "npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
